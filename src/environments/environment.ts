// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  BASE_API_URL: "http://localhost/admin/api/",
  BASE_API_URL_LIVE: "https://myshopfinder.com/admin/api/",
  BASE_TITLE:"Shop the best fashion in one place | Shopping made easy | MyShopFinder",
  BASE_DESCRIPTION:"Shop the best fashion in one place. Carefully hand selected brands and the best products and prices only",
  COMMON_TITLE:" | Shop the best fashion in one place | MyShopFinder",
  LIMIT_RECORD:16
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
